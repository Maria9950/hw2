#pragma once

class MathModule {
public:
    // a + b
    int add(int a, int b);

    // a - b
    int sub(int a, int b);

    // a * b
    int mul(int a, int b);

    // a * b + c
    int linear(int a, int b, int c);
};
